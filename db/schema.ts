import { sql } from "drizzle-orm";
import { integer, real, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const productos = sqliteTable("productos", { clave: text("clave").primaryKey(), descripcion: text("descripcion").notNull(), caracteristicas: text("caracteristicas").notNull().default(""), departamento: text("departamento").notNull(), categoria: text("categoria").notNull(), precio: real("precio").notNull().default(0), existenciaSicarx: integer("existencia_sicarx").notNull().default(0) });
export const escaneos = sqliteTable("escaneos", { id: integer("id").primaryKey({ autoIncrement: true }), codigoCaja: text("codigo_caja").notNull(), codigoEscaneado: text("codigo_escaneado").notNull(), descripcion: text("descripcion").notNull(), caracteristicas: text("caracteristicas").notNull().default(""), departamento: text("departamento").notNull(), categoria: text("categoria").notNull(), precio: real("precio").notNull().default(0), ubicacion: text("ubicacion").notNull().default(""), usuario: text("usuario").notNull(), fechaEscaneo: text("fecha_escaneo").notNull().default(sql`CURRENT_TIMESTAMP`) });
